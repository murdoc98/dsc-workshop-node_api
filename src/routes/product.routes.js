// import packages
const { Router } = require('express');

// Import controllers
const prodcut = require('../controllers/products.controller');

// Create instance
const router = Router();


module.exports = router;